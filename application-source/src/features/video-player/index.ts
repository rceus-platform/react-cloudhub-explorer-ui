/**
 * Video Player Feature Entry Point
 *
 * Responsibilities:
 * - Export feature-specific components and types
 * - Act as the public interface for the video player domain
 *
 * Boundaries:
 * - Does not export internal service methods or hook implementations
 */

export * from "./components/VideoPlayer";
export * from "./types";
